{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "python: current file",
            "type": "shell",
            "command": "${command:python.interpreterPath}",
            "args": ["${file}"],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}